{{!< default}}
<iframe id="news-frame" class="custom-iframe" title="admin-news"></iframe>

<script>

    let PageNews = `${integrationFrontend}/newsV2`;
    console.log(PageNews);
    let elementId = 'news-frame';
    let attrKey = 'src';
    let attrValue = PageNews;

    addValueToAttr(elementId, attrKey, attrValue);
</script>